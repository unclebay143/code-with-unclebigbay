import { WhiteArea } from '@/components/dashboard/white-area';
import React from 'react';

type Props = {};

const Page = (props: Props) => {
  return <WhiteArea border>Help centres</WhiteArea>;
};

export default Page;
